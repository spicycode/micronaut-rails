* tweak micronaut-rails example_helper so it will detect being run in a vendor/rails context, and use micronaut from vendor/rails if its there...otherwise fall back to normal gem setup (i think will-paginate does this, check it for an example)
